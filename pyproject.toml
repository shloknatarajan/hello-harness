[project]
name = "hello-harness"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "litellm",
]

[dependency-groups]
dev = [
    "python-dotenv>=1.2.2",
    "ruff>=0.15.8",
]
